# Tiny Tapeout project information
project:
  title:        "Motion Detect"      # Project title
  author:       "Lincy"      # Your name
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "Motion Detection"      # One line description of what your project does
  language:     "Analog" # other examples include Verilog, Amaranth, VHDL, etc
  clock_hz:     0       # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "1x2"          # Valid values for analog projects: 1x2, 2x2

  # How many analog pins does your project use?
  analog_pins: 2        # Valid values: 0 to 6
  uses_3v3: false       # Set to true if your project uses 3.3V (VAPWR) in addition to 1.8V (VDPWR)

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module: "tt_um_motion_detect"

  # List your project's source files here. Source files must be in ./src and you must list each source file separately, one per line:
  source_files:        
    - "tt_um_motion_detect.v"
    - "fifo.v"
    - "grayscale.v"
    - "grayscale_top.v"
    - "background_subtract.v"
    - "background_subtract_top.v"
    - "highlight.v"
    - "highlight_top.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "clock"
  ui[1]: "reset"
  ui[2]: "frame_wr_en"
  ui[3]: "frame_din"
  ui[4]: "background_wr_en"
  ui[5]: "background_din"
  ui[6]: "output_rd_en"
  ui[7]: ""

  # Outputs
  uo[0]: "frame_full"
  uo[1]: "background_full"
  uo[2]: "output_dout"
  uo[3]: "output_empty"
  uo[4]: ""
  uo[5]: ""
  uo[6]: ""
  uo[7]: ""

  # Bidirectional pins
  uio[0]: "VGND"
  uio[1]: "VDPWR"
  uio[2]: ""
  uio[3]: ""
  uio[4]: ""
  uio[5]: ""
  uio[6]: ""
  uio[7]: ""

  # Analog pins - make sure to also set "analog_pins" above, else the pins won't be connected
  ua[0]: ""
  ua[1]: ""
  ua[2]: ""
  ua[3]: ""
  ua[4]: ""
  ua[5]: ""

# Do not change!
yaml_version: 6
